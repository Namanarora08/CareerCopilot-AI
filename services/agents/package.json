{
  "name": "@careercopilot/agents",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "lint": "echo '✓ No issues found in services/agents (mock lint)'",
    "build": "echo '✓ Build succeeded for services/agents (mock build)'",
    "dev": "echo '✓ Service agents running in stub mode (mock dev)'"
  }
}
