# CareerCopilot — Desktop Application (`apps/desktop`)

Desktop shell wrapper for CareerCopilot Core built with Rust and Tauri.

## Overview
Wraps the responsive Next.js frontend compiled assets with lightweight OS level bridges, allowing desktop notifications, filesystem system tray, and peripheral integrations in future iterations.
